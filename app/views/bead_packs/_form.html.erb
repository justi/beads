<%= form_for(@bead_pack) do |f| %>
  <% if @bead_pack.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bead_pack.errors.count, "error") %> prohibited this bead_pack from being saved:</h2>

      <ul>
      <% @bead_pack.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
  <% end %>
  
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>

  <div class="field">
  <%= f.collection_select :bead_type_id, BeadType.find_each, :id, :name, prompt: "Select a Bead Type" %> 
  or create one: <%= f.text_field :new_type_name %>
  </div> 

  <div class="field">
  <%= f.collection_select :size_id, Size.find_each, :id, :name, prompt: "Select a Bead Size" %>
  or create one: <%= f.text_field :new_size_name %>
  </div>
  
  <div class="field">
    <%= f.label :count %><br>
    <%= f.text_field :count %>
  </div>
  <!--<div class="field">-->
  
  <!--</div>-->
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
