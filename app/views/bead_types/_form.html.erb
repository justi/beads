<%= form_for(@bead_type) do |f| %>
  <% if @bead_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bead_type.errors.count, "error") %> prohibited this bead_type from being saved:</h2>

      <ul>
      <% @bead_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
